import {Inter} from 'next/font/google';
import {Lusitana} from 'next/font/google'
export const inter = Inter({ subsets: ['latin'] });
export const inter1 = Lusitana({subsets:['latin'], weight:"700"});